services:
  - type: web
    name: rapid-response-crisis
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
